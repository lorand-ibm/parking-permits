version: "3.9"

services:
  api:
    build:
      context: .
      target: development_stage
    volumes:
      - .:/app:cached
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - DEBUG=true
      - DEV_SERVER=true
      - DJANGO_SECRET_KEY=NotImportantHere
